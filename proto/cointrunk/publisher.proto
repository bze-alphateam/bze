syntax = "proto3";
package bze.cointrunk;

option go_package = "github.com/bze-alphateam/bze/x/cointrunk/types";

message Publisher {
  
  string name = 1; 
  string address = 2; 
  bool active = 3;
  uint32 articlesCount = 4;
  int64 createdAt = 5;
}
